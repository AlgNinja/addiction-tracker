{
  "name": "addiction-tracker-monorepo",
  "version": "1.0.0",
  "description": "Full-stack Addiction Tracker with React, Node/Express, and PostgreSQL (Docker or Render)",
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "start": "node backend/src/server.js",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "backend:dev": "npm --prefix backend run dev",
    "frontend:dev": "npm --prefix frontend run dev"
  }
}
